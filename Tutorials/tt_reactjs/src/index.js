// Comienza la web

import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
// App es el componente que se desea ejecutar
// root es el elemento del DOM sobre el que se desea actuar
